version: 0.2

phases:
  install:
    commands:
      - npm install
      - cd client
      - npm install
  build:
    commands:
      - npm run build
artifacts:
  files:
    - client/build/**/*
    - .ebextensions/**/*
    - .elasticbeanstalk/**/*
    - .settings/**/*
    - commands/**/*
    - raw/**/*
    - routes/**/*
    - views/**/*
    - autocomp/**/*
    - '*'
